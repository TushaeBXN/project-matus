# Local binaries — never commit
.bin/

# Model weights — large files, use Git LFS or download scripts instead
.models/*.gguf
.models/ollama_storage/

# Python
__pycache__/
*.pyc
*.pyo

# Logs
*.log

# Conversation memory (personal — not committed)
.matus_memory.json

# Training data — keep local
data/raw_responses.jsonl
data/math_tutor_dataset.jsonl
data/matus_finetune.jsonl
data/eval_*.json

# Student session logs — never commit
tutor/data/sessions/
tutor/data/student_memory/

# Training artifacts
matus-adapter/
matus-3b/
matus-3b_gguf/
